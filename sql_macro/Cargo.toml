[package]
name = "sql_macro"
version = "0.1.0"
edition = "2021"
description = "Neon Postgres WASI HTTP driver"
license = "Apache-2.0"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["full"] }
quote = "1.0"
